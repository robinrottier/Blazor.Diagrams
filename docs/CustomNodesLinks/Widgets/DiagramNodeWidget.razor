@using CustomNodesLinks.Models

<div class="graphml-node @(Node != null && Node.Selected ? " graphml-selected" : "")">
  @Node?.Name
</div>

@code {

  [Parameter]
  public DiagramNode? Node { get; set; }

}
